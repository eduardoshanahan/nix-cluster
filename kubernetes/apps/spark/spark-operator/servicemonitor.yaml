apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: spark-operator
  namespace: spark
  labels:
    app: spark-operator
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: spark-kubernetes-operator
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
