apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: traefik

resources:
  - namespace.yaml

helmCharts:
  - name: traefik
    releaseName: traefik
    repo: https://traefik.github.io/charts
    version: 39.0.5
    namespace: traefik
    valuesFile: values.yaml
