apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: traefik

resources:
  - namespace.yaml

helmCharts:
  - name: traefik
    releaseName: traefik
    repo: https://traefik.github.io/charts
    version: 41.0.1
    namespace: traefik
    valuesFile: values.yaml
