# Networking Platform Workloads

This directory holds shared cluster networking components.

Current components:

- `metallb`
- `traefik`

Build the manifests with:

```bash
nix run .#render-platform
```
