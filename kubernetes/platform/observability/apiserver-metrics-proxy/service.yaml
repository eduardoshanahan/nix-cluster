apiVersion: v1
kind: Service
metadata:
  name: apiserver-metrics-proxy
  namespace: observability
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: apiserver-metrics-proxy
  ports:
    - name: http
      port: 8080
      targetPort: http
