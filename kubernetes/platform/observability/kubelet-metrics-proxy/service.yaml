apiVersion: v1
kind: Service
metadata:
  name: kubelet-metrics-proxy
  namespace: observability
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kubelet-metrics-proxy
  ports:
    - name: http
      port: 8080
      targetPort: http
