apiVersion: apps/v1
kind: Deployment
metadata:
  name: control-plane-metrics-proxy
  namespace: observability
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: control-plane-metrics-proxy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: control-plane-metrics-proxy
    spec:
      serviceAccountName: control-plane-metrics-proxy
      containers:
        - name: proxy
          image: python:3.13-alpine
          imagePullPolicy: IfNotPresent
          command:
            - python
            - /app/proxy.py
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: PORT
              value: "8080"
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
            timeoutSeconds: 5
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
          volumeMounts:
            - name: proxy-script
              mountPath: /app
              readOnly: true
      volumes:
        - name: proxy-script
          configMap:
            name: control-plane-metrics-proxy
            defaultMode: 0555
